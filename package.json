{
  "name": "mpr-slice-renderer",
  "version": "1.0.0",
  "description": "3D Mesh 切面实时渲染模块 (MPR Slice Renderer)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "@webgpu/types": "^0.1.69",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "clipper2-wasm": "^0.2.1",
    "manifold-3d": "^3.3.2"
  }
}
