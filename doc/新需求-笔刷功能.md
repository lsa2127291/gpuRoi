## 笔刷功能
首先选定一个 mesh 作为 activeMesh，笔刷可以修改 activeMesh 的数据
### 两阶段 + 预提交

1. `mousemove`：当前视图预览笔刷轨迹和当前 active 的 mesh 切面的布尔运算结果
2. `mouseup`：把结果的改变落到 mesh 的数据上，如果其他视图就要接着刷新其他视图的勾画根据新的mesh数据显示
3. 笔刷精度是 0.1 mm，最大半径为 50mm，move 的过程要求性能 < 30ms，落数据 + 刷新其他视图 < 500ms
